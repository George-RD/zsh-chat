# Track: 002-cli-skeleton Plan

## Objective
Create a basic zsh script that acts as the CLI entry point for `zsh-chat`.

## Plan
1.  [x] Research simple zsh script structures for CLI tools.
2.  [x] Implement a basic script at `client/zsh-chat.zsh` that:
    -   Displays a welcome message.
    -   Handles a `--help` or `-h` flag.
    -   Accepts a `post` command (mocking the behavior for now).
3.  [x] Make the script executable.
4.  [x] Create a basic `Makefile` (or similar) to facilitate development/installation.
5.  [x] Test the basic skeleton.
6.  [x] Commit the CLI skeleton.
